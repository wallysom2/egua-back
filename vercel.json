{
  "version": 2,
  "buildCommand": "npm run build && npm run prisma:generate",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}

